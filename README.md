# Reservation System - Vercel Version

ระบบจองคิวออนไลน์สำหรับแผนกฝากครรภ์และวางแผนครอบครัว

## ✅ สถานะ: พร้อม Deploy!

**index.html ได้รับการแก้ไขเรียบร้อยแล้ว!**
- ✅ เปลี่ยนจาก `google.script.run` → `fetch()` API
- ✅ เรียก `/api/slots` สำหรับตรวจสอบ time slots
- ✅ เรียก `/api/booking` สำหรับบันทึกการจอง

## ไฟล์ในโปรเจค:

- `index.html` - Frontend (✅ แก้ไขเรียบร้อย!)
- `api/slots.js` - API สำหรับตรวจสอบ time slots ที่ว่าง
- `api/booking.js` - API สำหรับบันทึกการจอง
- `package.json` - Node.js dependencies
- `vercel.json` - Vercel configuration

## วิธี Deploy:

### ขั้นตอนที่ 1: เตรียม Google Service Account

ดูรายละเอียดใน `DEPLOY-GUIDE.md` (Phase 1)

### ขั้นตอนที่ 2: Push ขึ้น GitHub

1. ไปที่ GitHub repository: `reservation-system` 
2. ลบไฟล์เก่าทั้งหมด
3. อัปโหลดไฟล์ทั้งหมดจากโฟลเดอร์นี้
4. Commit

### ขั้นตอนที่ 3: Deploy ไป Vercel

1. ไปที่ https://vercel.com
2. Sign up/Login ด้วย GitHub
3. Import repository: `reservation-system`
4. **ตั้ง Environment Variable:**
   - Name: `GOOGLE_CREDENTIALS`
   - Value: วาง JSON key ทั้งหมดจาก Service Account
5. Deploy!

## API Endpoints:

### GET /api/slots

ตรวจสอบ time slots ที่ว่าง

Parameters:
- `date` (required): วันที่ในรูปแบบ YYYY-MM-DD
- `department` (optional): ชื่อแผนก

Response:
```json
{
  "09:30-10:30": { "count": 3, "available": true },
  "10:30-11:30": { "count": 10, "available": false },
  ...
}
```

### POST /api/booking

บันทึกการจอง

Body:
```json
{
  "date": "2025-12-20",
  "time": "09:30-10:30",
  "department": "แผนกฝากครรภ์",
  "name": "ชื่อ-นามสกุล",
  "phone": "0812345678"
}
```

Response:
```json
{
  "success": true,
  "message": "บันทึกการจองเรียบร้อยแล้ว"
}
```

## 🎉 ข้อดีของ Vercel Version:

- ✅ ไม่มี Google Auth แฝง
- ✅ ไม่มีปัญหาหลายบัญชี Google
- ✅ เร็วกว่า Google Apps Script มาก
- ✅ ไม่มีปัญหา cache
- ✅ Custom domain ได้ฟรี
- ✅ HTTPS automatic

# reservation-system
